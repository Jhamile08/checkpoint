<% content_for :title,  t('publications.edit') %>

<h1 class="text-3xl font-bold text-center my-6"><%= t('publications.edit') %></h1>

<%= render "form", publication: @publication %>

<br>

<div class="text-center mt-6">
  <%= link_to t('publications.show'), @publication, class: "inline-block bg-white text-blue-600 px-6 py-2 border border-blue-600 rounded-lg hover:bg-blue-100 transition-colors" %> |
  <%= link_to t('publications.back_to_publication'), publications_path, class: "inline-block bg-gray-600 text-white px-6 py-2 rounded-lg hover:bg-gray-700 transition-colors" %>
</div>
